# 📖 מדריך שימוש

## הרצת המערכת

### בחירת שלב

המערכת מורכבת משלושה שלבים. כל שלב בונה על הקודם:

**שלב א' - MVP**: חיפוש סמנטי בסיסי
```bash
python main_stage_a.py
```

**שלב ב' - Event-Driven**: ארכיטקטורה משופרת
```bash
python main_stage_b.py
```

**שלב ג' - Hybrid**: מערכת מלאה (מומלץ)
```bash
python main_stage_c.py
```

או השתמש בתפריט:
```bash
python run_all_stages.py
```

## שימוש בממשק

### 1. הזנת נתיב הפרויקט

כשהמערכת מתחילה, תתבקש להזין נתיב לפרויקט:
```
Enter the path to your project with agentic tools docs: C:\path\to\your\project
```

### 2. המתנה לטעינה

המערכת תבצע:
- טעינת מסמכים
- יצירת embeddings
- אינדוקס ב-Pinecone
- (שלב ג') חילוץ נתונים מובנים

זה יכול לקחת מספר דקות בפעם הראשונה.

### 3. פתיחת הממשק

כשמוכן, תראה:
```
Running on local URL:  http://127.0.0.1:7860
```

פתח את הכתובת בדפדפן.

## שאילת שאלות

### טיפים לשאלות טובות

✅ **עשה:**
- שאל שאלות ספציפיות
- השתמש במונחים מהתיעוד
- שאל שאלה אחת בכל פעם

❌ **אל תעשה:**
- שאלות מעורפלות
- שאלות מרובות בבת אחת
- שאלות על קוד ספציפי (רק על תיעוד)

### דוגמאות לשאלות

**כלליות:**
```
איך מתקינים את המערכת?
מה הארכיטקטורה?
איך עובד ה-authentication?
```

**עיצוב:**
```
מה הצבע העיקרי?
איזה UI framework נבחר?
איך מטפלים ב-RTL?
```

**טכניות:**
```
איזה database נבחר?
מה מבנה ה-API?
איך מטפלים בשגיאות?
```

**מובנות (שלב ג'):**
```
תן לי רשימה של כל ההחלטות
מה ההנחיה העדכנית לגבי RTL?
אילו אזהרות נוספו בשבוע האחרון?
```

## הבנת התשובות

### מבנה התשובה

כל תשובה כוללת:
1. **התשובה עצמה** - טקסט מפורט
2. **מקורות** - מאיפה המידע הגיע
3. **רמת ביטחון** (שלב ב' ואילך)
4. **סוג שאילתה** (שלב ג')

### דוגמה לתשובה

```
התשובה: המערכת משתמשת ב-PostgreSQL כמסד נתונים ראשי...

מקורות:
1. Windsurf - decisions.md (Score: 0.87)
2. Cursor - spec.md (Score: 0.82)

רמת ביטחון: 85%
סוג שאילתה: semantic
```

### פענוח Score

- **0.8-1.0**: רלוונטיות גבוהה מאוד
- **0.6-0.8**: רלוונטיות טובה
- **0.4-0.6**: רלוונטיות בינונית
- **< 0.4**: רלוונטיות נמוכה (תקבל אזהרה)

## תכונות מתקדמות (שלב ג')

### סיווג אוטומטי

המערכת מזהה אוטומטית:
- שאלות כלליות → חיפוש סמנטי
- "תן לי רשימה" → שליפה מובנית
- "העדכני/האחרון" → מידע עדכני
- "בשבוע האחרון" → מבוסס זמן

### סינון

אפשר לסנן לפי:
```
הצג החלטות הקשורות ל-backend
אזהרות ברמת חומרה גבוהה
כללים של UI
```

## שאלות נפוצות

**ש: כמה זמן לוקח להריץ בפעם הראשונה?**
ת: 2-5 דקות, תלוי בכמות המסמכים.

**ש: האם צריך להריץ מחדש כל פעם?**
ת: לא. ה-Index נשמר ב-Pinecone. בהרצה הבאה זה יהיה מהיר יותר.

**ש: מה קורה אם המסמכים משתנים?**
ת: צריך להריץ מחדש כדי לעדכן את ה-Index.

**ש: אפשר להשתמש בלי אינטרנט?**
ת: לא. צריך חיבור ל-Cohere ו-Pinecone.

**ש: כמה עולה?**
ת: החשבונות החינמיים מספיקים לשימוש בסיסי.

## טיפים לשימוש יעיל

1. **התחל עם שאלות כלליות** ואז התמקד
2. **בדוק את המקורות** לוודא רלוונטיות
3. **שים לב לרמת הביטחון** - אם נמוכה, נסח מחדש
4. **השתמש בהיסטוריה** - גלול למעלה לראות שאלות קודמות
5. **נקה היסטוריה** כשמתחיל נושא חדש

## פתרון בעיות במהלך שימוש

**תשובות לא רלוונטיות:**
- נסח את השאלה אחרת
- היה יותר ספציפי
- בדוק שהמסמכים מכילים את המידע

**שגיאות:**
- רענן את הדף
- בדוק חיבור לאינטרנט
- ראה `docs/troubleshooting.md`

**איטיות:**
- זה תקין בשאלה הראשונה
- שאלות הבאות יהיו מהירות יותר
