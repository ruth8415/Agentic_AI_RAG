# יומן עבודה - סוכן Claude

## עדכון מתאריך 10/03/2026
ביצעתי שינוי במבנה ה-DB. הוספתי שדה בשם `is_completed` לטבלת המשימות.
הסרתי את השדה הישן `status_code` כי הוא היה מיותר.

## מגבלות טכניות
קיימת מגבלה בשרת הנוכחי - לא ניתן להעלות קבצים שגדולים מ-5MB. זה נושא שחוזר בכמה מסמכים וצריך לטפל בו.